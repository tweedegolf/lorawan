use std::time::Duration;

pub const RX1_DELAY: Duration = Duration::from_secs(1);
pub const RX2_DELAY: Duration = Duration::from_secs(2);

pub const JOIN_ACCEPT_1_DELAY: Duration = Duration::from_secs(5);
pub const JOIN_ACCEPT_2_DELAY: Duration = Duration::from_secs(6);
